import React from 'react';
import ReactDOM from 'react-dom/client'; // Importando a versão correta para React 18+
import App from './App';

// Criando a raiz
const root = ReactDOM.createRoot(document.getElementById('root'));

// Renderizando o App
root.render(<App />);
